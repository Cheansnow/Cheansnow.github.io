# 音乐添加说明

播放器固定在页面左下角。它显示当前歌曲的封面，封面中央是播放/暂停按钮；接近页面顶部或页脚时会自动隐藏。

## 添加自己的音乐

1. 打开 GitHub 仓库 `Cheansnow/Cheansnow.github.io`。
2. 切换到 `hexo-source` 分支。
3. 把音乐、封面和可选歌词上传到 `source/music/` 文件夹。
4. 打开根目录的 `_config.redefine.yml`。
5. 找到 `plugins` → `aplayer` → `audios`。
6. 在现有歌曲下面继续添加一组配置，然后提交修改。

```yaml
      - name: 歌曲名称
        artist: 歌手名称
        url: /music/歌曲文件名.mp3
        cover: /music/封面文件名.jpg
        lrc: /music/歌词文件名.lrc
        theme: "#A31F34"
```

没有歌词时，保留空值：

```yaml
        lrc:
```

也可以填写公开的音乐直链：

```yaml
        url: https://example.com/music/song.mp3
        cover: https://example.com/music/cover.jpg
```

`url` 必须直接指向音频文件，不能填写音乐平台的分享页面。建议使用 `.mp3` 音频、`.jpg` 或 `.webp` 封面、`.lrc` 歌词，并只上传你有权使用的音乐。
